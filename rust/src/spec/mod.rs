//! The specification, generated from Lean.
//!
//! `model.rs` is produced by `scripts/lean_to_verus.py` from `Dpss/IntModel.lean`
//! and must not be edited by hand; CI regenerates it and fails if it has drifted.

pub mod model;
